# 🚀 Quick Start: MCP Pipeline Agent

## За 5 минут

### Шаг 1: Запустите MCP серверы (30 сек)

```bash
cd mcp_servers
start_all_REAL.bat  # Запустит реальный пошук + файловий сервер
```

Или вручную:
```bash
# Терминал 1 - Реальний пошук (DuckDuckGo)
python search_real.py

# Терминал 2 - Файловий сервер
python filesystem_demo.py
```

### Шаг 2: Откройте приложение (10 сек)

1. Запустите ChatAgent в Android Studio
2. Нажмите иконку дерева 🌳 в топ-баре
3. Вы увидите экран Pipeline Agent

### Шаг 3: Запустите пайплайн (30 сек)

1. Выберите **"Web Search & Save"**
2. Посмотрите на шаги пайплайна (раскроется список)
3. Нажмите **"Run"**
4. Наблюдайте за выполнением в реальном времени

### Шаг 4: Проверьте результат (10 сек)

Откройте файл: `mcp_servers/output/ai_summary.txt`

## Что произошло?

```
┌─────────────────────────────────────────┐
│ 1. РЕАЛЬНИЙ пошук (DuckDuckGo)         │
│    Запрос: "latest AI developments"     │
│    Результат: 3 РЕАЛЬНІ статьі з інету │
└─────────────────┬───────────────────────┘
                  │ ${PREVIOUS_OUTPUT}
                  ▼
┌─────────────────────────────────────────┐
│ 2. Суммаризация                         │
│    Вход: Текст из 3 статей              │
│    Результат: Краткое резюме 500 симв.  │
└─────────────────┬───────────────────────┘
                  │ ${PREVIOUS_OUTPUT}
                  ▼
┌─────────────────────────────────────────┐
│ 3. Сохранение (File System)            │
│    Вход: Текст резюме                   │
│    Результат: Файл ai_summary.txt       │
└─────────────────────────────────────────┘
```

## Созданные файлы

### Основной код

```
app/src/main/java/com/example/chatagent/
├── data/remote/client/
│   └── MultiMcpClient.kt          # Клиент для нескольких MCP серверов
├── domain/
│   ├── model/Pipeline.kt          # Модели данных пайплайна
│   └── usecase/
│       └── ExecutePipelineUseCase.kt  # Агент-оркестратор
└── presentation/
    └── pipeline/
        ├── PipelineViewModel.kt   # ViewModel с готовыми пайплайнами
        └── PipelineScreen.kt      # UI экран
```

### MCP серверы (РЕАЛЬНИЙ пошук)

```
mcp_servers/
├── search_real.py            # Реальний Web Search (DuckDuckGo) - порт 3000
├── filesystem_demo.py        # File System MCP (порт 3001)
├── requirements.txt          # Python зависимости
├── start_all_REAL.bat        # Запуск всех серверов (Windows)
├── start_real_search.bat     # Запуск тільки пошуку
├── start_filesystem.bat      # Запуск тільки файлової системи
├── test_real_search.py       # Тест реального пошуку
└── output/                   # Папка для сохранения результатов
```

### Документация

```
├── MCP_PIPELINE_README.md          # Обзор проекта
├── PIPELINE_SETUP_GUIDE.md         # Подробный гайд по настройке
├── PIPELINE_IMPLEMENTATION.md      # Детали реализации
└── QUICK_START_PIPELINE.md         # Этот файл
```

## Ключевые особенности

### ✅ РЕАЛЬНИЙ пошук в інтернеті
- DuckDuckGo + Wikipedia
- Без API ключа
- Безкоштовно
- Актуальні результати

### ✅ Мульти-сервер клиент
Управляет несколькими MCP серверами одновременно

### ✅ Автоматическая передача данных
```kotlin
arguments = mapOf(
    "content" to "\${PREVIOUS_OUTPUT}"  // Использует результат предыдущего шага
)
```

### ✅ Real-time прогресс
```
[14:30:15] Pipeline started
[14:30:16] Executing: Search Web (real internet search)
[14:30:18] ✓ Search Web completed
[14:30:18]   Output: Found 3 REAL articles...
```

### ✅ Обработка ошибок
При ошибке на любом шаге пайплайн останавливается с сохранением промежуточных результатов

## Создание своего пайплайна

В `PipelineViewModel.kt`:

```kotlin
private fun createMyPipeline(): PipelineConfig {
    return PipelineConfig(
        name = "My Custom Pipeline",
        description = "What it does",
        steps = listOf(
            PipelineStep(
                name = "Step 1: Do something",
                serverUrl = "http://10.0.2.2:3000",
                toolName = "my_tool",
                arguments = mapOf("param" to "value"),
                order = 1
            ),
            PipelineStep(
                name = "Step 2: Process result",
                serverUrl = "http://10.0.2.2:3001",
                toolName = "another_tool",
                arguments = mapOf(
                    "input" to "\${PREVIOUS_OUTPUT}"  // Использует Step 1
                ),
                order = 2
            )
        )
    )
}
```

Добавьте в список:
```kotlin
val demoPipelines = listOf(
    createSearchAndSavePipeline(),
    createWebScraperPipeline(),
    createMyPipeline()  // ← добавьте здесь
)
```

## Требования

- ✅ Android 8.0+ (API 26+)
- ✅ Python 3.7+ (для MCP серверов)
- ✅ Минимум 2 MCP сервера
- ✅ Internet permission в AndroidManifest.xml
- ✅ `usesCleartextTraffic="true"` для HTTP

## Troubleshooting

### Приложение не подключается к серверам
1. Проверьте, что серверы запущены:
   ```bash
   curl http://localhost:3000
   curl http://localhost:3001
   ```

2. Для эмулятора используйте: `http://10.0.2.2:3000`
3. Для физического устройства: `http://YOUR_IP:3000`

### Python ошибки
```bash
cd mcp_servers
pip install -r requirements.txt
```

### Порты заняты
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

## Следующие шаги

1. **Попробуйте второй пайплайн**: "Web Scraper Pipeline"

2. **Создайте свой пайплайн**: Используйте шаблон выше

3. **Добавьте новые инструменты**: Расширьте MCP серверы

4. **Интегрируйте реальные API**: Brave Search API, Claude API и т.д.

## Полезные ссылки

- **MCP Protocol**: https://spec.modelcontextprotocol.io/
- **Brave Search API**: https://brave.com/search/api/
- **Claude API**: https://anthropic.com/

## Поддержка

Проблемы? Смотрите:
- `PIPELINE_SETUP_GUIDE.md` - детальный гайд
- `PIPELINE_IMPLEMENTATION.md` - технические детали
- `mcp_servers/README.md` - про MCP серверы

## Демо видео

1. Запуск серверов
2. Открытие Pipeline экрана
3. Выбор и запуск пайплайна
4. Просмотр логов выполнения
5. Проверка результата

---

**Готово!** 🎉 Вы только что запустили автоматический пайплайн из 3 MCP инструментов.

Теперь попробуйте создать свой пайплайн для автоматизации ваших задач!
