# MCP Servers для Pipeline Agent

## 🌐 РЕАЛЬНИЙ пошук в інтернеті!

Ці сервери виконують **справжній** пошук через DuckDuckGo + Wikipedia.

## Швидкий старт

### 1. Встановіть залежності (один раз)

```bash
pip install -r requirements.txt
```

### 2. Запустіть обидва сервери

**Windows:**
```bash
start_all_REAL.bat
```

Відкриються 2 вікна:
- **Real Web Search** (порт 3000) - пошук в інтернеті
- **File System** (порт 3001) - робота з файлами

**Або окремо:**
```bash
# Терминал 1
start_real_search.bat

# Терминал 2
start_filesystem.bat
```

**Або вручну (Linux/Mac):**
```bash
# Терминал 1
python search_real.py

# Терминал 2
python filesystem_demo.py
```

### 3. Перевірте роботу

```bash
test_real_search.bat   # Windows
python test_real_search.py   # Linux/Mac
```

Очікуваний результат:
```
✓ Search completed in 1.5s
✓ Results contain REAL URLs (not demo data)
🎉 REAL SEARCH IS WORKING!
```

### 4. Використайте в додатку

1. Відкрийте ChatAgent
2. Натисніть іконку дерева 🌳
3. Оберіть "Web Search & Save"
4. Натисніть "Run"

## Сервери

### 1. Real Web Search (порт 3000)

**Файл:** `search_real.py`

**Що робить:**
- Справжній пошук через DuckDuckGo
- Запасний варіант: Wikipedia
- Без API ключа
- Безкоштовно

**Інструменти:**
- `brave_web_search(query, count)` - пошук в інтернеті
- `summarize(text, max_length)` - суммаризація тексту

**Приклад використання:**
```python
# Будь-який запит працює!
search("latest SpaceX news")
search("kotlin android tips")
search("machine learning 2024")
```

### 2. File System (порт 3001)

**Файл:** `filesystem_demo.py`

**Що робить:**
- Читання/запис файлів
- Список файлів у директорії
- Автоматична конвертація Android шляхів

**Інструменти:**
- `write_file(path, content)` - записати файл
- `read_file(path)` - прочитати файл
- `list_directory(path)` - список файлів

**Папка результатів:** `output/`

## URL для додатку

### Android Emulator:
```kotlin
"http://10.0.2.2:3000"  // Web Search
"http://10.0.2.2:3001"  // File System
```

### Фізичний пристрій:
Дізнайтесь IP вашого комп'ютера:
```bash
# Windows
ipconfig

# Linux/Mac
ifconfig
```

Потім використайте:
```kotlin
"http://192.168.1.100:3000"  // Замініть на ваш IP
"http://192.168.1.100:3001"
```

## Логи

Сервери виводять детальні логи:

```
[MCP] Received: tools/call
[Tool] brave_web_search: query='latest AI', count=3
[Search] Searching DuckDuckGo for: 'latest AI'
[Search] Found 3 results
[Tool] Returning 3 results
```

Це означає що **реальний пошук відбувся**!

## Результати

Файли зберігаються в `output/`:
```
output/
├── ai_summary.txt
├── test_file.txt
└── ...
```

## Troubleshooting

### "Connection refused"
**Проблема:** Додаток не може підключитись до серверів

**Рішення:**
1. Перевірте, що сервери запущені (логи в вікнах)
2. Для емулятора використовуйте `10.0.2.2`, не `localhost`
3. Переконайтесь, що `usesCleartextTraffic="true"` в AndroidManifest.xml

### "No results found"
**Проблема:** DuckDuckGo не повертає результати

**Рішення:**
1. Це нормально іноді (rate limiting)
2. Почекайте 10-20 секунд
3. Спробуйте інший запит
4. Сервер автоматично перемкнеться на Wikipedia

### Повільний пошук
**Проблема:** Запит займає 2-3 секунди

**Рішення:**
Це нормально для реального пошуку:
- Запит до DuckDuckGo: ~500-2000ms
- Парсинг результатів: ~100-300ms
- Якщо потрібна швидкість, використайте Brave API

### Порти зайняті
**Windows:**
```bash
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

**Linux/Mac:**
```bash
lsof -ti:3000 | xargs kill
```

## Файли

```
mcp_servers/
├── search_real.py           # Реальний пошук (DuckDuckGo + Wikipedia)
├── filesystem_demo.py       # Файловий сервер
├── requirements.txt         # Python залежності
├── start_all_REAL.bat       # Запуск всіх серверів (Windows)
├── start_real_search.bat    # Запуск тільки пошуку
├── start_filesystem.bat     # Запуск тільки файлів
├── test_real_search.py      # Тест реального пошуку
├── test_real_search.bat     # Запуск тесту
└── output/                  # Результати
```

## Технічні деталі

### DuckDuckGo Search

**Як працює:**
1. HTTP запит до `https://html.duckduckgo.com/html/`
2. Парсинг HTML через BeautifulSoup
3. Витягування заголовків, URL, описів
4. Повернення JSON результатів

**Переваги:**
- ✅ Безкоштовно
- ✅ Без API ключа
- ✅ Реальні актуальні дані

**Обмеження:**
- ⚠ Rate limiting (може блокувати при багатьох запитах)
- ⚠ Іноді не повертає результати

### Wikipedia Fallback

Якщо DuckDuckGo не працює, автоматично використовується Wikipedia API:

```python
response = requests.get(
    'https://en.wikipedia.org/w/api.php',
    params={
        'action': 'opensearch',
        'search': query,
        'limit': 3
    }
)
```

## Розширення

### Додавання інших пошукових систем

Можна додати Google, Bing, або інші:

```python
# В search_real.py
def search_google(query):
    # Використайте Google Custom Search API
    pass

def search_bing(query):
    # Використайте Bing Search API
    pass
```

### Інтеграція Brave API

Для найкращої якості можна підключити Brave Search API:

1. Отримайте ключ: https://brave.com/search/api/
2. Змініть `search_real.py`:

```python
BRAVE_API_KEY = "ваш_ключ"

response = requests.get(
    'https://api.search.brave.com/res/v1/web/search',
    headers={'X-Subscription-Token': BRAVE_API_KEY},
    params={'q': query, 'count': count}
)
```

## Підтримка

Питання? Проблеми?
- Дивіться логи серверів
- Запустіть `test_real_search.py`
- Перевірте інтернет з'єднання

---

🎉 **Готово! Тепер у вас реальний пошук в інтернеті!**
