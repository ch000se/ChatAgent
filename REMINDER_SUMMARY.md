# Reminder Summary - Автоматична система сповіщень

## Що це?

Система автоматично надсилає summary всіх нагадувань у визначений час.
Працює 24/7 навіть коли додаток закритий.

## Компоненти

1. **MCP Server** (`mcp_reminder_server.py`) - зберігає нагадування в SQLite
2. **DailySummaryWorker** - автоматично генерує summary і показує notification

## Налаштування часу

Відкрий `app/src/main/java/com/example/chatagent/MyApp.kt`:

```kotlin
companion object {
    const val DAILY_SUMMARY_HOUR = 9      // Година (0-23)
    const val DAILY_SUMMARY_MINUTE = 0    // Хвилина (0-59)
}
```

Після зміни перезапусти додаток.

## Використання

### 1. Запусти MCP сервер
```bash
python mcp_reminder_server.py
```

### 2. Додай нагадування
- Відкрий додаток
- Натисни іконку 🔌 (MCP Connection)
- Вибери сервер "Localhost Reminder Server (LAN)" або введи URL: `http://192.168.0.100:3000/mcp`
- Натисни **Connect**
- Викликай tool `add_reminder`:
  ```json
  {
    "title": "Назва задачі",
    "description": "Опис",
    "priority": "high"
  }
  ```

**Важливо**: Якщо емулятор не підключається:
1. Перевір що MCP сервер працює: `python mcp_reminder_server.py`
2. Додай правило в Windows Firewall для Python/порту 3000
3. Для емулятора спробуй `http://10.0.2.2:3000/mcp`
4. Для реального пристрою використай IP комп'ютера в мережі

### 3. Очікуй notification
У налаштований час прийде сповіщення з summary всіх нагадувань.
Форматується Claude з емодзі та статистикою.

## Як це працює

```
[Запланований час]
       ↓
[DailySummaryWorker запускається]
       ↓
[Підключення до MCP сервера]
       ↓
[Виклик get_summary tool → отримання даних з SQLite]
       ↓
[Відправка даних до Claude для форматування]
       ↓
[Показ notification з красивим summary]
```

## Перевірка

Дивись logcat при запуску додатку:
```
⏰ DAILY SUMMARY SCHEDULER
📅 Scheduled time: 9:00
⏱️  Next run in: 45m 23s
```

Notification прийде автоматично у визначений час.
