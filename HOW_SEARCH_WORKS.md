# 🔍 Як працює пошук в Pipeline Agent

## ⚡ Поточна ситуація: РЕАЛЬНИЙ пошук (DuckDuckGo)

### Що зараз відбувається (РЕАЛЬНИЙ пошук!)

```
┌────────────────────────────────────────┐
│  Android App                            │
│  "latest AI developments 2024"          │
└─────────────────┬──────────────────────┘
                  │ HTTP POST
                  ▼
┌────────────────────────────────────────┐
│  search_real.py (порт 3000)            │
│                                         │
│  1. Запит до DuckDuckGo                │
│     https://html.duckduckgo.com        │
│                                         │
│  2. Парсинг HTML результатів           │
│     (реальні заголовки, URL, описи)    │
│                                         │
│  3. Якщо DuckDuckGo не відповів:       │
│     → Wikipedia API (запасний варіант) │
│                                         │
│  ✓ Повертає 3 РЕАЛЬНІ статті з інету  │
└─────────────────┬──────────────────────┘
                  │ JSON Response (реальні дані!)
                  ▼
┌────────────────────────────────────────┐
│  Android App отримує результат          │
│  → Суммаризація                         │
│  → Збереження у файл                    │
└────────────────────────────────────────┘
```

### Як отримуються реальні результати?

**1. Запит з додатка:**
```kotlin
// В PipelineViewModel.kt
arguments = mapOf(
    "query" to "latest AI developments 2024",  // ← Любий запит!
    "count" to 3
)
```

**2. Сервер шукає в інтернеті:**
```python
# В search_real.py
def search_duckduckgo(query, num_results=3):
    # Запит до DuckDuckGo
    response = requests.get('https://html.duckduckgo.com/html/', params={'q': query})

    # Парсинг HTML результатів
    soup = BeautifulSoup(response.text, 'html.parser')

    # Витягуємо заголовки, URL, описи
    for result in soup.find_all('div', class_='result'):
        title = result.find('a', class_='result__a').text
        url = result.find('a', class_='result__a')['href']
        ...
```

**3. Повертає РЕАЛЬНІ дані з інтернету:**
- Актуальні заголовки статей
- Справжні URL (TechCrunch, MIT, ArXiv тощо)
- Реальні описи та контент

## Реальний пошук (DuckDuckGo + Wikipedia)

### Поточна реалізація ✅

**Переваги:**
- ✅ Будь-які запити
- ✅ Актуальні дані з інтернету
- ✅ Свіжі новини та статті
- ✅ Реальні URL та контент

**Недоліки:**
- ❌ Потрібен API ключ
- ❌ Ліміти запитів (2000/місяць на безкоштовному плані)
- ❌ Потрібен інтернет

## Як працює пайплайн (крок за кроком)

### Шаг 1: Підключення до серверів

```kotlin
// ExecutePipelineUseCase.kt
multiMcpClient.connectToServer("http://10.0.2.2:3000")  // Brave Search
multiMcpClient.connectToServer("http://10.0.2.2:3001")  // File System
```

### Шаг 2: Виконання кроку 1 - Пошук

```kotlin
// Крок 1: Пошук
val result = multiMcpClient.callTool(
    serverUrl = "http://10.0.2.2:3000",
    toolName = "brave_web_search",
    arguments = mapOf(
        "query" to "latest AI developments 2024",
        "count" to 3
    )
)

// Результат:
previousOutput = """
📄 GPT-5 Announced: The Next Generation of AI
🔗 https://example.com/gpt5
📝 OpenAI announces GPT-5...

📄 Google's Gemini Ultra Surpasses Human Experts
🔗 https://example.com/gemini
📝 Google's latest AI model...

📄 AI Regulation: New Global Framework Established
🔗 https://example.com/regulation
📝 World leaders agree...
"""
```

### Шаг 3: Виконання кроку 2 - Суммаризація

```kotlin
// Крок 2: Суммаризація
// ${PREVIOUS_OUTPUT} замінюється на результат кроку 1
val result = multiMcpClient.callTool(
    serverUrl = "http://10.0.2.2:3000",
    toolName = "summarize",
    arguments = mapOf(
        "text" to previousOutput,  // ← Тут увесь текст з кроку 1
        "max_length" to 500
    )
)

// Результат: перші 500 символів
previousOutput = "Summary (490 chars): 📄 GPT-5 Announced..."
```

### Шаг 4: Виконання кроку 3 - Збереження

```kotlin
// Крок 3: Збереження
val result = multiMcpClient.callTool(
    serverUrl = "http://10.0.2.2:3001",
    toolName = "write_file",
    arguments = mapOf(
        "path" to "/sdcard/Download/ai_summary.txt",
        "content" to previousOutput  // ← Суммаризований текст
    )
)

// Файл збережено в: mcp_servers/output/ai_summary.txt
```

## Як перейти на реальний пошук

### Варіант 1: Brave Search API

**1. Отримайте API ключ:**
```
https://brave.com/search/api/
```

**2. Запустіть сервер з API:**
```bash
# Windows
set BRAVE_API_KEY=ваш_ключ_тут
python brave_search_real.py

# Linux/Mac
export BRAVE_API_KEY=ваш_ключ_тут
python brave_search_real.py
```

**Тепер будуть реальні результати з інтернету!**

### Варіант 2: Інші API

Можна інтегрувати будь-який інший API:

**Google Custom Search:**
```python
response = requests.get(
    'https://www.googleapis.com/customsearch/v1',
    params={
        'key': GOOGLE_API_KEY,
        'cx': SEARCH_ENGINE_ID,
        'q': query
    }
)
```

**DuckDuckGo (безкоштовно, але неофіційно):**
```python
from duckduckgo_search import DDGS

with DDGS() as ddgs:
    results = list(ddgs.text(query, max_results=count))
```

**Bing Search API:**
```python
response = requests.get(
    'https://api.bing.microsoft.com/v7.0/search',
    headers={'Ocp-Apim-Subscription-Key': BING_API_KEY},
    params={'q': query, 'count': count}
)
```

## Додавання власних демо-запитів

Якщо не потрібен реальний пошук, можна додати свої запити:

```python
# В brave_search_demo.py
DEMO_RESULTS = {
    "latest AI developments 2024": [...],
    "machine learning trends": [...],

    # Додайте свої запити:
    "kotlin android tips": [
        {
            "title": "Top 10 Kotlin Tips for Android",
            "url": "https://example.com/kotlin",
            "description": "Best practices for Kotlin development",
            "content": "Kotlin has become the preferred language..."
        }
    ],

    "jetpack compose tutorial": [
        {
            "title": "Jetpack Compose Guide",
            "url": "https://example.com/compose",
            "description": "Modern UI toolkit for Android",
            "content": "Jetpack Compose simplifies UI development..."
        }
    ]
}
```

Потім в додатку:
```kotlin
// В PipelineViewModel.kt
arguments = mapOf(
    "query" to "kotlin android tips",  // ← Ваш новий запит
    "count" to 3
)
```

## Схема даних

```
┌─────────────────────────────────────────────────────────────┐
│                      DEMO MODE (зараз)                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  App → MCP Server → DEMO_RESULTS словник → Відповідь        │
│                                                              │
│  Затримка: ~10ms                                            │
│  Вартість: $0                                               │
│  Результати: Статичні, передвстановлені                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    REAL API MODE                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  App → MCP Server → Brave API → Інтернет → Відповідь       │
│                                                              │
│  Затримка: ~500-2000ms                                      │
│  Вартість: Безкоштовно до 2000 запитів/місяць              │
│  Результати: Актуальні, з реального пошуку                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Висновок

**Зараз використовується демо-режим:**
- Статичні дані для швидкого тестування
- Працює "з коробки" без налаштувань
- Ідеально для розробки та демонстрації пайплайну

**Для production:**
- Інтегруйте Brave Search API (або інший)
- Отримайте реальні результати з інтернету
- Підтримка будь-яких запитів

**Головне:** Архітектура пайплайну однакова для обох режимів - просто міняєте сервер!
