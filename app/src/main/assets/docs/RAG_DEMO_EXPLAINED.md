# 🎓 RAG: Перший запит та порівняння з відповіддю без RAG

## 📘 1. Коротко: що таке RAG у цьому прикладі

**RAG (Retrieval-Augmented Generation)** — це архітектурний патерн, де LLM отримує **додатковий контекст** з релевантних документів перед генерацією відповіді.

**Простими словами:**
- 🟦 **Без RAG**: LLM відповідає лише на основі знань, закладених під час тренування
- 🟩 **З RAG**: LLM + знайдені релевантні частини документів = більш точна, конкретна відповідь

**У нашому прикладі:**
```
База знань (умовна):
├─ документ_1.txt: "Політика відпусток CompanyX: співробітники отримують 20 днів оплачуваної відпустки..."
├─ документ_2.txt: "Remote work policy: всі розробники можуть працювати віддалено..."
└─ документ_3.txt: "Salary review process: перегляд зарплати відбувається щороку в січні..."
```

---

## 🔄 2. Пайплайн RAG (покроково)

### **Етап 1: Індексація (один раз при завантаженні документів)**

```python
# Псевдокод для IndexDocumentUseCase

def index_document(document_content: str):
    # Крок 1.1: Розбити документ на чанки (chunks)
    chunks = split_into_chunks(
        text=document_content,
        chunk_size=500,      # максимум 500 символів на чанк
        overlap=50           # перекриття 50 символів між чанками
    )

    # Крок 1.2: Згенерувати векторні ембедінги для кожного чанка
    vectorizer = TfidfVectorizer()
    vectorizer.fit(chunks)  # навчити на всіх чанках

    embeddings = []
    for chunk in chunks:
        # TF-IDF векторизація: перетворює текст у вектор чисел
        embedding = vectorizer.transform(chunk)
        embeddings.append({
            'text': chunk,
            'vector': embedding,  # наприклад: [0.2, 0.0, 0.7, 0.1, ...]
            'chunk_index': len(embeddings)
        })

    # Крок 1.3: Зберегти чанки та їх ембедінги в БД
    save_to_database(embeddings)

    return f"Indexed {len(chunks)} chunks"
```

**Реальна реалізація в проєкті:**
- `DocumentRepositoryImpl:285-298` — функція `chunkText()`
- `DocumentRepositoryImpl:303-305` — функція `generateEmbedding()`
- `DocumentRepositoryImpl:62-145` — повний процес індексації

---

### **Етап 2: Пошук релевантних чанків (при кожному запиті користувача)**

```python
# Псевдокод для SearchDocumentsUseCase

def search_relevant_chunks(user_query: str, top_k: int = 3):
    # Крок 2.1: Згенерувати ембедінг для запиту користувача
    query_embedding = vectorizer.transform(user_query)

    # Крок 2.2: Отримати всі збережені чанки з БД
    all_chunks = get_all_chunks_from_database()

    # Крок 2.3: Порахувати схожість між запитом і кожним чанком
    results = []
    for chunk in all_chunks:
        similarity = cosine_similarity(
            query_embedding,
            chunk['vector']
        )
        results.append({
            'text': chunk['text'],
            'similarity': similarity,
            'document_name': chunk['document_name']
        })

    # Крок 2.4: Сортувати за схожістю і взяти топ-K
    ranked_results = sorted(results, key=lambda x: x['similarity'], reverse=True)
    top_results = ranked_results[:top_k]

    return top_results
```

**Що таке косинусна подібність?**
```
Вектор запиту:     [0.5, 0.3, 0.8, 0.1]
Вектор чанка #1:   [0.6, 0.2, 0.7, 0.0]  → similarity = 0.95 (дуже схожі!)
Вектор чанка #2:   [0.1, 0.0, 0.1, 0.9]  → similarity = 0.23 (не схожі)

Чим ближче до 1.0 — тим більша схожість.
```

**Реальна реалізація:**
- `DocumentRepositoryImpl:147-212` — функція `searchDocuments()`
- `DocumentRepositoryImpl:307-325` — функція `cosineSimilarity()`

---

### **Етап 3: Формування промпта з контекстом**

```python
# Псевдокод для RAG-запиту до LLM

def rag_query(user_question: str):
    # Крок 3.1: Знайти релевантні чанки
    relevant_chunks = search_relevant_chunks(user_question, top_k=3)

    # Крок 3.2: Сформувати контекст з топ-3 чанків
    context = "\n\n".join([
        f"[Документ: {chunk['document_name']}]\n{chunk['text']}"
        for chunk in relevant_chunks
    ])

    # Крок 3.3: Побудувати промпт для LLM
    prompt = f"""
    Ти — асистент компанії. Відповідай ТІЛЬКИ на основі наданого контексту.
    Якщо відповіді немає в контексті — скажи "Інформація відсутня".

    КОНТЕКСТ:
    {context}

    ПИТАННЯ: {user_question}

    ВІДПОВІДЬ:
    """

    # Крок 3.4: Відправити промпт до LLM
    response = call_llm(prompt)

    return response, relevant_chunks
```

---

## 🔴 3. Режим 1 — Відповідь БЕЗ RAG

### Приклад запиту:
```
Питання користувача: "Скільки днів відпустки я маю у CompanyX?"
```

### Промпт до LLM (без контексту):
```
Ти — AI-асистент. Відповідай на питання користувача.

ПИТАННЯ: Скільки днів відпустки я маю у CompanyX?

ВІДПОВІДЬ:
```

### Відповідь LLM:
```
Я не маю доступу до внутрішніх політик CompanyX. Зазвичай компанії
пропонують від 10 до 25 днів оплачуваної відпустки на рік, залежно від:
- Країни (трудове законодавство)
- Стажу роботи
- Позиції

Рекомендую звернутися до HR-відділу або переглянути ваш трудовий договір.
```

### Аналіз відповіді:
| Критерій | Оцінка | Коментар |
|----------|--------|----------|
| **Точність** | ⚠️ Загальна | Немає конкретної цифри для CompanyX |
| **Конкретність** | ❌ Низька | Розпливчаста відповідь "10-25 днів" |
| **Корисність** | 🟡 Середня | Надає загальну інформацію, але не відповідає на питання |
| **Галюцинації** | ✅ Немає | LLM чесно визнає, що не знає |
| **Залежність від контексту** | ❌ Критична | Потребує зовнішньої інформації |

---

## 🟢 4. Режим 2 — Відповідь З RAG

### Той самий запит:
```
Питання користувача: "Скільки днів відпустки я маю у CompanyX?"
```

### Крок 1: Пошук релевантних чанків
```python
query = "Скільки днів відпустки я маю у CompanyX?"
results = search_relevant_chunks(query, top_k=3)

# Результат пошуку:
[
    {
        'text': 'Політика відпусток CompanyX: всі штатні співробітники отримують 20 днів оплачуваної відпустки на рік. Після 3 років роботи — 25 днів. Відпустка розраховується пропорційно від дати найму.',
        'similarity': 0.87,
        'document_name': 'vacation_policy.txt'
    },
    {
        'text': 'Remote work policy: всі розробники можуть працювати віддалено до 3 днів на тиждень. Для повного віддаленого режиму потрібне узгодження з менеджером.',
        'similarity': 0.34,
        'document_name': 'remote_work.txt'
    },
    {
        'text': 'Salary review process: перегляд зарплати відбувається щороку в січні на основі performance review.',
        'similarity': 0.12,
        'document_name': 'salary_review.txt'
    }
]
```

**Аналіз пошуку:**
- Топ-1 чанк (similarity=0.87) — **ДУЖЕ РЕЛЕВАНТНИЙ**, містить пряму відповідь
- Топ-2 чанк (similarity=0.34) — слабка релевантність (слово "днів" викликало схожість)
- Топ-3 чанк (similarity=0.12) — майже не релевантний

---

### Крок 2: Формування промпта з контекстом
```
Ти — асистент компанії CompanyX. Відповідай ТІЛЬКИ на основі наданого контексту.
Якщо відповіді немає в контексті — скажи "Інформація відсутня в базі знань".

КОНТЕКСТ:
[Документ: vacation_policy.txt]
Політика відпусток CompanyX: всі штатні співробітники отримують 20 днів
оплачуваної відпустки на рік. Після 3 років роботи — 25 днів. Відпустка
розраховується пропорційно від дати найму.

[Документ: remote_work.txt]
Remote work policy: всі розробники можуть працювати віддалено до 3 днів на тиждень.

[Документ: salary_review.txt]
Salary review process: перегляд зарплати відбувається щороку в січні.

ПИТАННЯ: Скільки днів відпустки я маю у CompanyX?

ВІДПОВІДЬ:
```

### Відповідь LLM (з RAG):
```
Згідно з політикою CompanyX, ви маєте право на:
- **20 днів** оплачуваної відпустки на рік (стандартна норма)
- **25 днів** — якщо ваш стаж у компанії перевищує 3 роки

Відпустка розраховується пропорційно від дати вашого найму.

Джерело: vacation_policy.txt
```

### Аналіз відповіді:
| Критерій | Оцінка | Коментар |
|----------|--------|----------|
| **Точність** | ✅ Висока | Конкретна цифра з офіційної політики |
| **Конкретність** | ✅ Висока | Чітка відповідь: 20 або 25 днів |
| **Корисність** | ✅ Висока | Повністю відповідає на питання + додатковий контекст |
| **Галюцинації** | ✅ Немає | LLM використав лише наданий контекст |
| **Залежність від контексту** | ✅ Знята | Контекст надано через RAG |

---

## 📊 5. Порівняльна таблиця

| Критерій | Без RAG 🟦 | З RAG 🟩 | Переможець |
|----------|-----------|----------|------------|
| **Точність відповіді** | ⚠️ Загальна інформація | ✅ Конкретні дані з документів | 🟩 RAG |
| **Конкретність** | ❌ "10-25 днів" (діапазон) | ✅ "20 або 25 днів" (точно) | 🟩 RAG |
| **Джерело інформації** | ⚠️ Тренувальні дані LLM | ✅ Офіційні документи компанії | 🟩 RAG |
| **Ризик галюцинацій** | 🟡 Середній (може вигадати) | ✅ Низький (обмежений контекстом) | 🟩 RAG |
| **Актуальність** | ❌ Дані можуть бути застарілі | ✅ Реальні документи (свіжі) | 🟩 RAG |
| **Швидкість** | ✅ Миттєва (лише LLM) | ⚠️ +200-500ms (пошук чанків) | 🟦 Без RAG |
| **Складність реалізації** | ✅ Проста (один API-запит) | ⚠️ Складна (індексація, векторний пошук) | 🟦 Без RAG |
| **Витрати** | ✅ Тільки LLM API | ⚠️ LLM API + векторна БД | 🟦 Без RAG |

---

## 🎯 6. Висновок: коли RAG виправданий, а коли — ні

### ✅ **RAG ВИПРАВДАНИЙ**, коли:

1. **Потрібна точність для domain-specific знань**
   ```
   ❌ Без RAG: "Зазвичай компанії дають 10-25 днів відпустки"
   ✅ З RAG:   "У CompanyX — 20 днів (за політикою від 01.01.2024)"
   ```

2. **Інформація часто змінюється**
   - Ціни, політики, регламенти → оновити документ легше, ніж перетренувати LLM

3. **Потрібна прозорість джерел (compliance, legal)**
   - RAG може цитувати: "Згідно з contract_v3.pdf, параграф 4.2..."

4. **Приватні/конфіденційні дані**
   - LLM не має таких знань → RAG додає з локальної БД

5. **Зменшення галюцинацій**
   - Інструкція: "Відповідай ТІЛЬКИ на основі контексту" → LLM не вигадує

---

### ❌ **RAG НЕ ПОТРІБЕН / ЗАЙВИЙ**, коли:

1. **Загальні знання (common knowledge)**
   ```
   Питання: "Що таке Python?"
   → LLM і так знає, RAG не додасть цінності
   ```

2. **Немає спеціфічних документів**
   - Якщо база знань порожня або нерелевантна → RAG поверне шум

3. **Потрібна креативність / генерація**
   ```
   Питання: "Напиши вірш про кота"
   → RAG обмежить креативність LLM
   ```

4. **Критична швидкість відповіді**
   - Real-time чат-боти (пошук чанків додає латентність)

5. **Прості запити без контексту**
   ```
   Питання: "Привіт, як справи?"
   → Звичайна розмова, RAG зайвий
   ```

6. **Високі витрати на підтримку**
   - Якщо документи оновлюються рідше 1 разу на місяць → можна fine-tune LLM замість RAG

---

## 📐 7. Реальний приклад з коду проєкту

### Як це працює в ChatAgent:

```kotlin
// 1. Індексація документа (один раз)
val documentContent = "Політика відпусток CompanyX: 20 днів..."
indexDocumentUseCase(documentId).collect { progress ->
    // Progress: "Processing chunk 1/3", "Processing chunk 2/3", ...
}

// 2. Пошук релевантних чанків (при кожному запиті)
val query = "Скільки днів відпустки?"
val results = searchDocumentsUseCase(query, topK = 3)

// 3. Формування контексту для LLM
val context = results.getOrNull()
    ?.joinToString("\n\n") { result ->
        "[${result.document.fileName}] ${result.chunk.text}"
    } ?: ""

// 4. Запит до LLM з контекстом
val prompt = """
    Контекст: $context

    Питання: $query

    Відповідь:
""".trimIndent()

val response = chatRepository.sendMessage(prompt)
```

**Файли для вивчення:**
- `DocumentRepositoryImpl.kt:147-212` — логіка пошуку
- `SearchDocumentsUseCase.kt:10-12` — entry point для пошуку
- `TfidfVectorizer.kt` — векторизація тексту (TF-IDF)

---

## 🧪 8. Додатковий приклад: коли RAG НАВМИСНЕ дав ГІРШИЙ результат

### Питання:
```
"Напиши креативну історію про AI-агента, який подорожує часом"
```

### Без RAG:
```
✅ Прекрасна історія на 500 слів з:
   - Оригінальним сюжетом
   - Творчими деталями
   - Неочікуваним фіналом
```

### З RAG (якщо в базі є технічна документація):
```
❌ "Згідно з документацією AI_agents.pdf, агенти не можуть подорожувати часом,
    оскільки вони працюють на основі трансформерів..."

   → LLM намагається дотримуватися контексту, який тут зовсім не релевантний
   → Креативність вбита "слухняністю" до контексту
```

**Урок**: RAG має бути **вибірковим** (є запит на факти? → RAG. Креатив? → без RAG)

---

## 📝 Підсумок для студента

| Що треба запам'ятати | Чому це важливо |
|---------------------|-----------------|
| RAG ≠ магія | Це просто додавання контексту до промпта |
| Векторний пошук — серце RAG | Без нього неможливо знайти релевантні чанки |
| TF-IDF vs Semantic embeddings | TF-IDF (простіше, швидше), BERT (точніше, складніше) |
| Top-K чанків | Не всі знайдені чанки релевантні (дивись на similarity!) |
| Chunking має значення | 500 символів з overlap 50 — збалансований підхід |
| RAG — не для всього | Для загальних знань і креативу — зайвий |

---

## 🚀 Наступні кроки

1. **Експеримент**: змінити `CHUNK_SIZE` з 500 на 1000 → що станеться з точністю?
2. **Покращення**: замінити TF-IDF на BERT embeddings → порівняти результати
3. **Гібридний режим**: запит до LLM + питання "чи потрібен RAG для цього запиту?"
4. **Метрики**: додати логування similarity scores для аналізу якості пошуку

---

**Автор**: AI-інженер & викладач
**Дата**: 2025-12-23
**Проєкт**: ChatAgent RAG System
