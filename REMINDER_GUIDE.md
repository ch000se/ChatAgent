# 📋 Reminder System - Complete Guide

## ✅ Що було реалізовано

### 1. Локальний MCP Reminder Server
- ✅ Python FastAPI сервер з 5 інструментами
- ✅ SQLite база даних для зберігання нагадувань
- ✅ Підтримка пріоритетів, дедлайнів, описів
- ✅ Автоматичний розрахунок overdue tasks

### 2. Android Integration
- ✅ WorkManager для періодичних перевірок (кожні 15 хвилин)
- ✅ Notification система для показу summary
- ✅ Автоматичний запуск при старті додатка
- ✅ Hilt dependency injection

---

## 🚀 Швидкий старт

### Крок 1: Запустити MCP Reminder Server

```bash
# Встановити залежності
pip install fastapi uvicorn

# Запустити сервер
python mcp_reminder_server.py
```

Побачите:
```
🚀 Starting MCP Reminder Server...
📍 Server will run on http://localhost:3000
📱 Android emulator: http://10.0.2.2:3000/mcp
```

### Крок 2: Підключитися з Android додатка

1. **Відкрийте додаток** → **MCP Tools**
2. **Виберіть** "Localhost (Recommended for Testing)"
3. **URL:** `http://10.0.2.2:3000/mcp`
4. **Натисніть** "Connect"
5. ✅ Повинні з'явитися 5 інструментів:
   - `add_reminder`
   - `list_reminders`
   - `complete_reminder`
   - `delete_reminder`
   - `get_summary`

### Крок 3: Дозволити нотифікації

При першому запуску додаток попросить дозвіл на нотифікації. **Дозвольте!**

---

## 💬 Як використовувати в чаті

### Додати нагадування

```
Додай нагадування купити молоко

Нагадай мені завтра зателефонувати Михайлу

Add reminder to finish project report by 2025-12-20

Create high priority task: prepare presentation
```

Claude автоматично викличе `add_reminder` з правильними параметрами!

### Переглянути всі нагадування

```
Покажи мої нагадування

What are my pending tasks?

List all reminders
```

### Позначити як виконане

```
Виконано нагадування номер 3

Mark reminder 5 as completed

Complete task 2
```

### Видалити нагадування

```
Видали нагадування 4

Delete reminder 1

Remove task 7
```

### Отримати summary

```
Дай summary моїх нагадувань

Show me reminder overview

What's my task status?
```

---

## 🛠️ Доступні інструменти

### 1️⃣ add_reminder
**Додає нове нагадування**

Параметри:
- `title` (обов'язково) - назва нагадування
- `description` (опціонально) - детальний опис
- `due_date` (опціонально) - дедлайн у форматі YYYY-MM-DD
- `priority` (опціонально) - "low", "medium", "high"

Приклад:
```json
{
  "title": "Finish report",
  "description": "Complete quarterly report for management",
  "due_date": "2025-12-25",
  "priority": "high"
}
```

### 2️⃣ list_reminders
**Показує всі нагадування**

Параметри:
- `show_completed` (опціонально) - показати виконані (default: false)

Приклад:
```json
{
  "show_completed": false
}
```

### 3️⃣ complete_reminder
**Позначає нагадування як виконане**

Параметри:
- `id` (обов'язково) - ID нагадування

Приклад:
```json
{
  "id": 3
}
```

### 4️⃣ delete_reminder
**Видаляє нагадування**

Параметри:
- `id` (обов'язково) - ID нагадування

Приклад:
```json
{
  "id": 5
}
```

### 5️⃣ get_summary
**Показує загальну статистику**

Параметри: немає

Приклад виводу:
```
📊 **Reminders Summary**

📝 Total pending: 5
⚠️ OVERDUE: 2
⏰ Due TODAY: 1
📅 Due this week: 2
🔴 High priority: 3

⚡ Action needed: You have overdue reminders!
```

---

## 🔔 Як працюють автоматичні нагадування

### Фонова робота

Android WorkManager автоматично:

1. **Перевіряє кожні 15 хвилин** (мінімум для Android)
2. **Підключається до MCP** сервера
3. **Викликає** `get_summary`
4. **Показує нотифікацію** якщо є pending tasks

### Налаштування періодичності

Знаходиться в `MyApp.kt:45`:

```kotlin
val reminderWorkRequest = PeriodicWorkRequestBuilder<ReminderWorker>(
    15, TimeUnit.MINUTES  // ← Тут можна змінити (мінімум 15 хвилин)
)
```

Для тестування можна використовувати OneTimeWorkRequest з меншими інтервалами.

### Умови запуску

Worker запускається лише коли:
- ✅ Є інтернет з'єднання (`NetworkType.CONNECTED`)
- ✅ MCP сервер підключений
- ✅ Є pending нагадування

---

## 📊 База даних

### Структура

SQLite база `reminders.db`:

```sql
CREATE TABLE reminders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    due_date TEXT,
    priority TEXT DEFAULT 'medium',
    completed INTEGER DEFAULT 0,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP
)
```

### Розташування

```
D:\AndroidStudioProjects\ChatAgent\reminders.db
```

### Переглянути базу

```bash
sqlite3 reminders.db

sqlite> SELECT * FROM reminders;
sqlite> .exit
```

---

## 🧪 Тестування

### 1. Тест додавання нагадування

**В чаті:**
```
Додай нагадування "Test task" з high priority
```

**Очікуваний результат:**
```
✅ Reminder added successfully! ID: 1
Title: Test task
Priority: HIGH
```

### 2. Тест отримання списку

**В чаті:**
```
Покажи мої нагадування
```

**Очікуваний результат:**
```
📋 **Your Reminders** (1 total)

⏳ [1] 🔴 **Test task**
```

### 3. Тест нотифікацій

1. Додайте кілька нагадувань
2. Зачекайте 1-2 хвилини (initial delay)
3. Повинна з'явитися нотифікація з summary

### 4. Ручна перевірка Worker

```kotlin
// В коді можна додати для тестування:
val workRequest = OneTimeWorkRequestBuilder<ReminderWorker>()
    .build()
WorkManager.getInstance(context).enqueue(workRequest)
```

---

## 📱 Структура проекту

```
app/src/main/java/com/example/chatagent/
├── notification/
│   └── NotificationHelper.kt       # Система нотифікацій
├── worker/
│   └── ReminderWorker.kt           # Фонова перевірка
└── MyApp.kt                         # Ініціалізація WorkManager

mcp_reminder_server.py               # MCP сервер
reminders.db                         # SQLite база
```

---

## 🔧 Troubleshooting

### Worker не запускається

**Перевірка:**
```kotlin
// Додати в код:
val workInfo = WorkManager.getInstance(context)
    .getWorkInfosForUniqueWork(ReminderWorker.WORK_NAME)
    .get()
Log.d("WorkManager", "Work state: ${workInfo.firstOrNull()?.state}")
```

**Рішення:**
- Переконайтеся що є інтернет
- Перевірте що MCP сервер запущений
- Перевірте логи: `adb logcat -s ReminderWorker`

### Нотифікації не показуються

**Перевірка:**
- Настройки → Додатки → ChatAgent → Нотифікації → Дозволено?

**Рішення:**
```kotlin
// Запитати дозвіл вручну:
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    requestPermissions(arrayOf(Manifest.permission.POST_NOTIFICATIONS), 1)
}
```

### MCP сервер недоступний

**Симптоми:**
- Worker завершується успішно але нічого не показує

**Рішення:**
- Переконайтеся що сервер запущений: `http://localhost:3000`
- Перевірте підключення в MCP Tools екрані
- Перезапустіть сервер

### База даних порожня

**Перевірка:**
```bash
sqlite3 reminders.db "SELECT COUNT(*) FROM reminders;"
```

**Рішення:**
- Додайте нагадування через чат
- Перевірте що MCP інструмент працює: Execute tool вручну

---

## 🎯 Приклади використання

### Щоденні задачі

```
Додай щоденні нагадування:
- Ранкова зарядка (низький пріоритет)
- Перевірити email (середній пріоритет)
- Standup meeting о 10:00 (високий пріоритет, дедлайн сьогодні)
```

### Проектні завдання

```
Створи нагадування для проекту:
- Code review - дедлайн 2025-12-20
- Write documentation - дедлайн 2025-12-22
- Deploy to production - дедлайн 2025-12-25 (високий пріоритет)
```

### Перегляд статусу

```
Дай мені summary нагадувань на сьогодні
```

---

## 🚀 Наступні покращення (опціонально)

1. **Категорії** - додати tags для групування
2. **Повторювані задачі** - weekly/daily reminders
3. **Snooze** - відкласти нагадування
4. **Rich notifications** - action buttons (Complete, Snooze)
5. **Widget** - показати summary на головному екрані
6. **Синхронізація** - Google Calendar integration
7. **Голосові нагадування** - TTS для нотифікацій

---

## ✅ Чеклист готовності

- [x] MCP сервер створено
- [x] SQLite база налаштована
- [x] WorkManager інтегровано
- [x] Notifications працюють
- [x] Claude може додавати reminders
- [x] Claude може показувати список
- [x] Claude може видаляти/завершувати
- [x] Автоматичні нотифікації кожні 15 хв
- [x] Документація створена

---

## 📖 Додаткові ресурси

- [WorkManager Guide](https://developer.android.com/topic/libraries/architecture/workmanager)
- [Notifications Guide](https://developer.android.com/develop/ui/views/notifications)
- [MCP Protocol Spec](https://modelcontextprotocol.io/)
- [FastAPI Docs](https://fastapi.tiangolo.com/)

---

**🎉 Готово! Тепер у вас є агент який працює 24/7 і нагадує про важливі задачі!**
