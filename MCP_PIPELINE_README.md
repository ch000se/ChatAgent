# MCP Pipeline Agent

## Что реализовано

Создана полноценная система для автоматической оркестрации нескольких MCP (Model Context Protocol) серверов в единую цепочку выполнения.

## Ключевые компоненты

### 1. MultiMcpClient
**Файл:** `app/src/main/java/com/example/chatagent/data/remote/client/MultiMcpClient.kt`

Клиент для управления несколькими MCP серверами одновременно:
- ✅ Подключение к нескольким серверам
- ✅ Управление сессиями
- ✅ Вызов инструментов на разных серверах
- ✅ Мониторинг состояния через StateFlow

### 2. ExecutePipelineUseCase
**Файл:** `app/src/main/java/com/example/chatagent/domain/usecase/ExecutePipelineUseCase.kt`

Агент-оркестратор для выполнения пайплайнов:
- ✅ Последовательное выполнение шагов
- ✅ Передача результатов между шагами (`${PREVIOUS_OUTPUT}`)
- ✅ Обработка ошибок
- ✅ Real-time прогресс через Flow

### 3. PipelineViewModel
**Файл:** `app/src/main/java/com/example/chatagent/presentation/pipeline/PipelineViewModel.kt`

ViewModel с готовыми демо-пайплайнами:
- ✅ Web Search & Save - поиск → суммаризация → сохранение
- ✅ Web Scraper - загрузка → извлечение → экспорт в JSON

### 4. PipelineScreen
**Файл:** `app/src/main/java/com/example/chatagent/presentation/pipeline/PipelineScreen.kt`

UI экран с:
- ✅ Список доступных пайплайнов
- ✅ Отображение шагов каждого пайплайна
- ✅ Кнопка запуска
- ✅ Real-time лог выполнения
- ✅ Отображение финального результата

### 5. Модели данных
**Файл:** `app/src/main/java/com/example/chatagent/domain/model/Pipeline.kt`

- `PipelineConfig` - конфигурация пайплайна
- `PipelineStep` - шаг пайплайна
- `PipelineExecutionResult` - результат выполнения
- `StepExecutionResult` - результат шага

## Как это работает

### Пример: Web Search & Save Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│  Шаг 1: Поиск в интернете (Brave Search MCP)              │
│  Input: "latest AI developments 2024"                      │
│  Output: 3 релевантные статьи                              │
└─────────────────┬───────────────────────────────────────────┘
                  │ ${PREVIOUS_OUTPUT}
                  ▼
┌─────────────────────────────────────────────────────────────┐
│  Шаг 2: Суммаризация (LLM/Claude)                         │
│  Input: Текст из 3 статей                                  │
│  Output: Краткое резюме (500 символов)                     │
└─────────────────┬───────────────────────────────────────────┘
                  │ ${PREVIOUS_OUTPUT}
                  ▼
┌─────────────────────────────────────────────────────────────┐
│  Шаг 3: Сохранение (File System MCP)                      │
│  Input: Текст резюме                                        │
│  Output: /sdcard/Download/ai_summary.txt                   │
└─────────────────────────────────────────────────────────────┘
```

## Необходимые MCP серверы (минимум 2)

### 1. Real Web Search Server (порт 3000) - РЕАЛЬНИЙ ПОШУК
Пошукова система: DuckDuckGo + Wikipedia
Інструменти: `brave_web_search`, `summarize`
**БЕЗ API ключа! Безкоштовно!**

```bash
cd mcp_servers
python search_real.py
```

### 2. File System MCP Server (порт 3001)
Инструменты: `write_file`, `read_file`, `list_directory`
```bash
cd mcp_servers
python filesystem_demo.py
```

## Документация

- **PIPELINE_SETUP_GUIDE.md** - подробное руководство по настройке MCP серверов
- **PIPELINE_IMPLEMENTATION.md** - детальное описание архитектуры и реализации

## Запуск

1. Запустите MCP серверы (автоматично):
   ```bash
   cd mcp_servers
   start_all_REAL.bat   # Windows
   ```

   Або вручну (2 термінала):
   ```bash
   # Терминал 1: Реальний Web Search (DuckDuckGo)
   python search_real.py

   # Терминал 2: File System
   python filesystem_demo.py
   ```

2. Откройте приложение и перейдите на экран Pipeline (иконка дерева 🌳 в топ-баре)

3. Выберите пайплайн "Web Search & Save" и нажмите "Run"

4. Наблюдайте за выполнением в реальном времени - отримаєте РЕАЛЬНІ результати з інтернету!

## Архитектура

```
┌──────────────────────────────────────────────────────────────┐
│                      PipelineScreen (UI)                      │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────────┐   │
│  │ Pipeline 1 │  │ Pipeline 2 │  │ Execution Progress    │   │
│  └────────────┘  └────────────┘  └──────────────────────┘   │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                    PipelineViewModel                          │
│  - availablePipelines                                        │
│  - executionProgress                                         │
│  - executePipeline()                                         │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│               ExecutePipelineUseCase (Orchestrator)          │
│  1. Connect to servers                                       │
│  2. Execute steps sequentially                               │
│  3. Pass results between steps                               │
│  4. Emit progress updates                                    │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                     MultiMcpClient                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Server 1     │  │ Server 2     │  │ Server N     │      │
│  │ (Brave)      │  │ (FileSystem) │  │ (...)        │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└──────────────────────────────────────────────────────────────┘
```

## Навигация

Добавлена кнопка перехода на Pipeline экран в `ChatScreen.kt`:
- Иконка: AccountTree (дерево)
- Расположение: TopAppBar

## Создание своих пайплайнов

В `PipelineViewModel.kt` добавьте новый метод:

```kotlin
private fun createMyPipeline(): PipelineConfig {
    return PipelineConfig(
        name = "My Pipeline",
        description = "What it does",
        steps = listOf(
            PipelineStep(
                name = "Step 1",
                serverUrl = "http://10.0.2.2:3000",
                toolName = "tool_name",
                arguments = mapOf("param" to "value"),
                order = 1
            ),
            PipelineStep(
                name = "Step 2",
                serverUrl = "http://10.0.2.2:3001",
                toolName = "another_tool",
                arguments = mapOf("input" to "\${PREVIOUS_OUTPUT}"),
                order = 2
            )
        )
    )
}
```

Затем добавьте в `loadDemoPipelines()`:
```kotlin
val demoPipelines = listOf(
    createSearchAndSavePipeline(),
    createWebScraperPipeline(),
    createMyPipeline()  // ← добавьте сюда
)
```

## Особенности реализации

### ✅ Передача данных между шагами
```kotlin
arguments = mapOf(
    "content" to "\${PREVIOUS_OUTPUT}"  // Автоматически заменяется
)
```

### ✅ Real-time прогресс
```kotlin
[14:30:15] Pipeline started
[14:30:16] Executing: Search Web
[14:30:18] ✓ Search Web completed
[14:30:18]   Output: Found 3 articles...
```

### ✅ Обработка ошибок
```kotlin
if (step fails) {
    emit(PipelineProgress.Failed)
    stop execution
    preserve intermediate results
}
```

### ✅ Мониторинг состояния серверов
```kotlin
serverStates: Map<String, ServerState>
// DISCONNECTED | CONNECTING | CONNECTED | ERROR
```

## Дальнейшее развитие

Потенциальные улучшения:
- ⏭ Условные переходы между шагами
- ⏭ Параллельное выполнение шагов
- ⏭ Повторные попытки при ошибках
- ⏭ Сохранение пайплайнов в БД
- ⏭ Визуальный редактор пайплайнов
- ⏭ Библиотека шаблонов

## Требования

- Android 8.0+ (API 26+)
- Минимум 2 запущенных MCP сервера
- Разрешения: INTERNET, WRITE_EXTERNAL_STORAGE
- `usesCleartextTraffic="true"` для HTTP

## Лицензия

Часть проекта ChatAgent

## Автор

Создано с использованием Claude Code
